<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <!-- For MaterialComponents, colorPrimaryVariant is often used for the status bar -->
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item> <!-- Text/icon color on primary color -->

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item> <!-- Text/icon color on secondary color -->

        <!-- colorAccent is an older attribute, Material Components use colorSecondary more. -->
        <!-- You can still define it if needed for older components or specific styling -->
        <item name="colorAccent">@color/teal_200</item>

        <!-- Status bar color (optional, but good practice with Material themes) -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

    </style>
    <!-- Base application theme for MoodJournal -->
    <style name="Theme.MoodJournal" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Example:
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorSecondary">@color/my_light_secondary</item>
        -->
    </style>
        </resources>